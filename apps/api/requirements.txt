fastapi==0.139.0
uvicorn[standard]==0.32.0
pydantic==2.13.4
pydantic-settings==2.14.2
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.13.3
PyJWT==2.13.0
bcrypt==4.2.0
redis==5.2.0
slowapi==0.1.9
python-dotenv==1.2.2
python-multipart==0.0.32

# wrapt: unpinned transitive dependency was causing pip-compile's resolver to
# non-deterministically pick between 2.2.2 and 2.3.0 on separate compile runs
# of the exact same inputs (confirmed via two parallel CI jobs on the same
# commit disagreeing, 2026-07-28). Pinned directly so resolution is stable.
wrapt==2.3.0

# OAuth (FINTRACK-42/43, ADR-016). google-auth verifies Google's ID
# tokens; cryptography backs PyJWT's RS256 support, needed to verify
# Apple's RS256-signed ID tokens; httpx fetches Apple's JWKS
# (oauth_verifier.AppleIdTokenVerifier) asynchronously.
#
# cryptography pinned to 48.0.1, not the first version that worked
# locally (43.0.3) -- CI's Trivy security scan flagged two HIGH CVEs on
# 43.0.3 (CVE-2026-26007, a subgroup-attack issue fixed in 46.0.5; and
# GHSA-537c-gmf6-5ccf, a vulnerable bundled OpenSSL fixed in 48.0.1).
# 48.0.1 clears both -- keep this at or above 48.0.1 on any future bump.
google-auth==2.35.0
cryptography==48.0.1
httpx==0.27.2
requests==2.34.2
