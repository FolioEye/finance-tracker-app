fastapi==0.139.0
uvicorn[standard]==0.32.0
pydantic==2.13.4
pydantic-settings==2.14.2
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.13.3
PyJWT==2.13.0
bcrypt==4.2.0
redis==5.2.0
slowapi==0.1.9
python-dotenv==1.2.2
python-multipart==0.0.32

# wrapt: unpinned transitive dependency was causing pip-compile's resolver to
# non-deterministically pick between 2.2.2 and 2.3.0 on separate compile runs
# of the exact same inputs (confirmed via two parallel CI jobs on the same
# commit disagreeing, 2026-07-28). Pinned directly so resolution is stable.
wrapt==2.3.0

# mako, starlette, cffi: same non-determinism class as wrapt above --
# unpinned, so separate pip-compile invocations on the identical inputs
# picked different versions minutes apart (FINTRACK-48, confirmed via two
# CI jobs in the same workflow run disagreeing). mako comes in
# via alembic, starlette via fastapi, cffi via cryptography -- none listed
# directly before this. Pinned to whatever the most recent resolve picked
# so the committed lock file and any fresh compile agree going forward.
mako==1.4.1
starlette==1.6.0
cffi==2.1.1

# greenlet, typing-inspection: same non-determinism class as wrapt/mako/starlette/cffi
# above -- unpinned, so separate pip-compile invocations on the identical inputs
# picked different versions minutes apart (PR #58, 2026-08-11). greenlet comes in
# via sqlalchemy[asyncio], typing-inspection via pydantic/pydantic-settings --
# neither listed directly before this.
greenlet==3.5.5
typing-inspection==0.4.3

# OAuth (FINTRACK-42/43, ADR-016). google-auth verifies Google's ID
# tokens; cryptography backs PyJWT's RS256 support, needed to verify
# Apple's RS256-signed ID tokens; httpx fetches Apple's JWKS
# (oauth_verifier.AppleIdTokenVerifier) asynchronously.
#
# cryptography pinned to 50.0.0 (bumped from 48.0.1 during QA Lead's
# FINTRACK-38 pass) -- CI's Trivy security scan flagged two more HIGH CVEs
# disclosed against 48.0.1 after it was pinned: CVE-2026-69247 (fixed
# 50.0.0) and CVE-2026-69249 (fixed 49.0.0). 50.0.0 clears both -- keep
# this at or above 50.0.0 on any future bump. See FINTRACK-48.
google-auth==2.35.0
cryptography==50.0.0
httpx==0.27.2
requests==2.34.2
